import type { Course } from '../types'

// Declared, not yet authored. The spine is visible in the app from day one; sections land here as
// the slice is authored. See COURSE-PLAN.md for the planned section list.
export const multipleFeatures: Course = {
  id: 'multiple-features',
  title: 'Many Features at Once',
  sections: [],
}
