import type { Section } from '../types'

export const theFullCycle: Section = {
  id: 'the-full-cycle',
  title: 'The full cycle',
  scene: 'the-full-cycle',
  slide: `## Zoom out: the model was one box of six

### Scoping is where projects are lost
Not "predict fraud" — *which decision changes, and what does being wrong cost?* Everything in the last three sections needed that answer and could not supply it.

### The label is a design decision
"Fraud" means chargeback filed, or analyst-confirmed, or customer-reported. Three different columns, three different models, and the dataset will not tell you which one it is.

### Monitor the inputs, not just the score
y arrives months late, or never. **Feature distributions arrive immediately** — and that is where drift shows first.

### Three that no validation split can show
An adversary who reads your decisions · a blocked transaction that never gets a label · a 50ms budget that turns a correct answer into a lost sale.`,
  narration:
    "Let us step all the way back and look at the shape of the whole project, because everything in this course so far — every loop, every diagnosis, every threshold — has been happening inside one box of six. The cycle goes: scope it, get the data, train the model, deploy it, monitor it, and go back to scoping. Scoping is first and it is where projects are most often lost, before a line of code exists. The failure is to scope as “predict fraud”, which sounds like a specification and is not one. The real questions are: which decision changes as a result of this model, and what does being wrong cost in each direction. Notice that the last three sections were completely unable to proceed without that answer. Precision or recall? Where on the curve? I kept saying it depends on what a false decline costs — and if that number does not exist, it is because nobody scoped the project properly, and you will end up inventing it yourself at a keyboard at six in the evening. Then data, and the part everyone skips: defining the label. What does y equal one mean? Does fraud mean a chargeback was filed, or that an analyst reviewed it and confirmed it, or that a customer rang up and said so? Those are three different columns. They have different volumes, different delays, and different biases, and a model trained on one of them will not answer a question asked about another. The dataset will not warn you; it will just contain whatever somebody decided in a meeting three years ago. Training is the four boxes from section one. Deployment is where it becomes a system: the model goes behind an API, it has to answer in fifty milliseconds, and that is a real constraint and not an ops detail, because a scoring call that misses its budget is a declined sale regardless of what the model would have said. Then monitoring, and here is the single most useful thing in this section. Everybody monitors accuracy in production. The problem is that in fraud, and in most rare-event problems, the truth arrives months late. A chargeback can take ninety days. So a metric dashboard tells you how you were doing last quarter, which is not a control system, it is a history lesson. Monitor the inputs instead. Feature distributions arrive immediately. If the average transaction amount shifts, or a merchant category suddenly triples in volume, or your velocity feature starts returning nulls because an upstream team changed a schema — you see all of that today, and every one of them will move your model before a single label comes back. And then three things that no validation split can ever show you, which are the three warning cards. The first is that your adversary adapts. This is not ordinary drift; fraud is not weather. There is a person on the other end whose job is to find out what you block and stop doing it, and the better your model is, the faster they learn. A fraud model decays because it works. The second is the feedback loop, and it is subtle and it is nasty. You block a transaction. What was it? You will never know. It did not happen, so it never got a chargeback, so it never got a label. Your model's own decisions are carving holes in the data you will use to train its replacement, and those holes are shaped exactly like the things it was most confident about. The third is latency, which I have already mentioned and which is worth repeating: fifty milliseconds, or the sale is gone. And then the arrow goes back to scoping, because what you learn in production is what you should have asked for at the start. A model is not a deliverable you hand over. It is a thing you keep.",
}
