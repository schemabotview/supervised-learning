import type { Section } from '../types'

export const boosting: Section = {
  id: 'boosting',
  title: 'Boosting',
  scene: 'boosting',
  slide: `## Build them in sequence, each one aimed at the leftover error

A forest builds trees **independently** and averages away their **variance.** Boosting builds them **one after another**, each fitted to the residual, and attacks **bias.**

### So the trees are stumps
Depth 2, not depth 12. Each contributes a nudge scaled by the learning rate, and the ensemble is a long sum of nudges rather than a vote.

### Read the flat start
Three rounds at 0.633 — which is exactly *predict "on time" for everything.* The log-odds has not yet moved anything past 0.5. Boosting begins as a no-op.

### It overfits too, slowly
Peak around round 60, then a long drift down while training accuracy keeps rising. **The number of rounds is the hyperparameter**, which is why every library ships early stopping.

### Which to reach for
A forest is hard to break — bad defaults cost you a little. Boosting wins more often and will overfit if you stop watching. **For tables, one of these two is still the answer.**`,
  narration:
    "There is a second way to use many trees, and it is close to the opposite idea. A random forest builds its trees in parallel and independently — no tree knows the others exist — and then averages them, and what the averaging removes is variance. Boosting builds its trees in sequence, and each one is built specifically to fix what the ones before it are still getting wrong. What that removes is bias. Here is the procedure. Start with a constant prediction: the overall rate of lateness, and nothing else. For every flight, compute the residual — how far off that prediction is. Then fit a small tree, not to the labels, but to those residuals. Add a fraction of that tree's output to the running prediction. Recompute the residuals, which are now smaller and concentrated on the flights the ensemble is still getting wrong. Fit another small tree to those. And keep going. Every tree in the sequence is aimed at whatever is left. The fraction I mentioned is the learning rate — ours is nought point three — and it is there so that no single tree can take over. And because each tree is only responsible for a correction rather than for the whole answer, the trees are tiny. Ours are depth two. Compare that with the forest, where we grew every tree to depth twelve. A forest is an average of strong models; boosting is a long sum of deliberately weak ones. Now look at the curve, and start at the left, because the first thing it does is surprising. It is flat. Three rounds, sitting at exactly nought point six three three, which is the dashed line — and the dashed line is the accuracy you get by predicting on-time for every single flight. For three rounds, boosting is doing nothing at all. That is not a bug. With a learning rate of nought point three and depth-two trees, the running score has not yet moved any flight far enough to cross the halfway line, so every prediction is still the majority class. Boosting starts as a no-op and has to accumulate. By round four it clears the line, by round twenty it is at nought point eight one six, and it reaches its peak of nought point eight three four around round sixty. Now keep watching past the peak, because this is the part that matters operationally. Held-out accuracy drifts down — slowly, not dramatically, but consistently — while training accuracy carries on climbing. Boosting overfits. It just takes its time about it. And that is a real difference from a random forest: with a forest you can add trees essentially forever and the held-out score flattens rather than declining, so the number of trees is not something you have to be careful about. With boosting it is the hyperparameter. Too few and you have stopped short; too many and you are fitting noise. This is exactly why every serious implementation ships early stopping — you hold out a validation set, you watch it each round, and you stop when it stops improving. Our own table has that in it: sixty rounds scores nought point eight three four and a hundred and twenty scores slightly less. The final table is every model from this course on the same five hundred and one held-out flights. One tree: nought point eight zero four, and it is the only one you can read. Bagging: eight one four. Random forest: eight four zero. Boosting at its peak: eight three four. And the honest reading of the last two is that they are the same — a six-thousandth of a difference on five hundred flights is not a result. In the wild, gradient boosting usually edges it, which is why XGBoost, LightGBM and CatBoost won the better part of a decade of tabular competitions. A forest is the more forgiving of the two: it is hard to break, it needs almost no tuning, and bad defaults cost you a little rather than a lot. Boosting will beat it more often than not, and it will also overfit if you stop paying attention. And the sentence to end the course on: for a table of rows and columns, one of those two is still the answer. Not a neural network. Trees split on one column at a time, they do not care about scaling, they take categorical columns and missing values in their stride, and they capture interactions for nothing — which is a very accurate description of what tabular data is actually like.",
}
